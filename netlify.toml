[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Note: For Netlify, you'll need to use Netlify Functions for the API proxy
# Create netlify/functions/claude.js for the serverless function
